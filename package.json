{
  "name": "bpmn-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter @bpmn/web dev",
    "lint": "pnpm -r lint",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
