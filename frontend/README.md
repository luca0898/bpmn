# Frontend

Workspace PNPM contendo:

- `apps/web`: aplicação React com Vite + TypeScript + TailwindCSS.
- `packages/ui`: pacote para componentes compartilhados (placeholder).
- `packages/editor`: pacote para editor BPMN (placeholder).
- `packages/workflow`: pacote para regras/workflow (placeholder).
